{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "MOPL#: Build Interpreter",
      "type": "shell",
      "command": "bash build.sh",
      "options": { "cwd": "${workspaceFolder}" },
      "group": "build",
      "problemMatcher": []
    },
    {
      "label": "MOPL#: Run Current File",
      "type": "shell",
      "command": "./mopl run \"${file}\"",
      "options": { "cwd": "${workspaceFolder}" },
      "group": { "kind": "test", "isDefault": true },
      "dependsOn": ["MOPL#: Build Interpreter"],
      "problemMatcher": [],
      "presentation": {
        "reveal": "always",
        "panel": "dedicated",
        "clear": true
      }
    }
  ]
}
